rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{uid}/sessions/{sessionId}/files/{file} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == uid;
    }
  }
}
